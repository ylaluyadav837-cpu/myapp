
import 'package:flutter/material.dart';

class SharesScreen extends StatelessWidget {
  const SharesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shares'),
      ),
      body: const Center(
        child: Text('Shares Screen'),
      ),
    );
  }
}
